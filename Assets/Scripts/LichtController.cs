using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LichtController : MonoBehaviour
{
    

// Start is called before the first frame update
void Start()
    {
        //LichtFR = GetComponent<Light>();
        //LichtFL = GetComponent<Light>();
        //LichtBR = GetComponent<Light>();
        //LichtBL = GetComponent<Light>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

